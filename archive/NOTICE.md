# Historical material

This folder preserves the original AWS/Streamlit prototype, patch bundles,
workbook, report script, and README. These are reference material, not maintained
entry points. Use the root README and the `beeplex` command for the current server.

The original app relies on the former root scripts and directory layout. Its
setup/deployment instructions do not apply to the reorganized package.
